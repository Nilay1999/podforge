# Backend
PORT=8080
APP_ENV=development        # development | production
LOG_LEVEL=info             # debug | info | warn | error
KUBECONFIG=~/.kube/config  # path to kubeconfig (omit when running in-cluster)
