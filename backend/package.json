{
  "name": "podforge-backend",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "mkdir -p bin && go build -o bin/server ./cmd/server",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "clean": "rm -rf bin"
  }
}
